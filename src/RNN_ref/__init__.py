"""Core SO-RaNN numerical helpers.

The modules in this package are kept compatible with the original experiment
scripts:

- ``RNN_funs``: randomized neural network bases, initialization, and
  divergence-free/curl-free activation helpers.
- ``sampling_points``: Gauss-Legendre, random, and uniform sampling routines.
- ``err_calculate``: quadrature-based norms and error metrics.
- ``fd``: finite-difference derivative approximations.
- ``plot_results``: shared plotting utilities.
"""

