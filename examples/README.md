# Examples

This folder contains lightweight entry points that exercise the repository
without running the full paper-scale experiments.

Run the smoke example from the repository root:

```bash
python examples/run_smoke.py
```

The paper-scale scripts are kept in `experiments/example4_*`.

